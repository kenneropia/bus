import * as userSchema from "./user/user.schema";
import * as productSchema from "./journey/journey.schema";
import * as cartSchema from "./cart/cart.schema";
const schema = { userSchema, productSchema, cartSchema };

export default schema;
